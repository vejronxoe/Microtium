#include"SaveAndLoad.h"

#include<iostream>
#include<fstream>
#include<sstream>

void WriteFile()
{

	
		
}


bool SaveBlocks(std::string path, std::vector<std::vector<Block>>& blocks)
{
	std::ofstream file(path, std::ios::binary);

	//file.write(reinterpret_cast<char*>(&d),)
}