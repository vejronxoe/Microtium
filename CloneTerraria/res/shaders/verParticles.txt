#version 330 core
layout (location = 0) in vec2 aPos;
layout (location = 1) in vec2 aTexCoord;

uniform mat4 camera;
uniform mat4 transform;
uniform mat4 scale;
uniform mat4 rotation;

void main()
{
	gl_Position =   camera * scale * rotation* transform * vec4(aPos.x, aPos.y, 0.0f, 1.0f);
}