#version 330 core
out vec4 FragColor;

in vec2 texCoord;
uniform int shadow;
uniform int lookAt;
uniform sampler2D ourTexture;

void main()
{
	FragColor = texture(ourTexture,  abs(texCoord - vec2(-0.5f * lookAt + 0.5f, 0))) - vec4(0.2 * shadow,0.2 * shadow,0.2 * shadow,0);
}